# QuiltOS Creator for Pixel 6a

## 0.1.7 — staged

This is the one-time OTA bootstrap release. It will add the live, exact-build
update feed and the persistent VM release operator. It remains unpublished
until build validation and an explicit device smoke test pass.

## 0.1.6 — installed test build

The recovery installer and ROM boot path were validated on the user's Pixel 6a.
Some bugs remain for 0.1.7 triage, and the first setup screen still resembles
the inherited wizard.
